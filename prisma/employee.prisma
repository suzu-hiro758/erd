model employee {
  id Int @id @default(autoincrement())
  name String
  company company @relation(fields: [companyId], references: [id])
  companyId Int
}

model company {
  id Int @id @default(autoincrement())
  name String
  employees employee[]
}
